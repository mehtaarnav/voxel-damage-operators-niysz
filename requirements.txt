# Pinned to the versions every result in the manuscript was produced with.
# Python 3.12.5.
#
# Two of these pins are load-bearing rather than routine:
#   porespy  -- a defect in its default parallel dispatch for snow2 affected the
#               Phase 3/4 network extractions; see IMPACT_NOTE_porespy_parallel_bug.md
#   numpy    -- the damage operators consume a seeded Generator stream, so a
#               change in the generator would change which voxels are chosen
#               and every damage figure with it
numpy==2.1.0
scipy==1.14.1
pandas==2.2.3
matplotlib==3.10.8
tifffile==2026.7.31
networkx==3.6.1
scikit-image==0.26.0
porespy==3.0.4
pytest==9.1.1
